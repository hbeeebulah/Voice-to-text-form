services:
  - type: web
    name: voxform-ai
    env: node
    plan: free
    buildCommand: npm run install:all && npm run build
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
      - key: VOICE_AI_API_KEY
        sync: false
